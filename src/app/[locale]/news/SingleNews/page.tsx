import { useTranslations } from 'next-intl';

const SingleNews = () => {
    const t = useTranslations('AboutUs');
    return (
        <div>
       
        </div>
    )
};

export default SingleNews
